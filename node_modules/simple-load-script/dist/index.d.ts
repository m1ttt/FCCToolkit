export interface Config {
    url: string;
    attrs?: Record<string, string>;
    inBody?: boolean;
    insertInto?: string | null;
    removeScript?: boolean;
}
export default function simpleLoadScript(config: (Config | string)[]): Promise<(HTMLScriptElement | undefined)[]>;
export default function simpleLoadScript(config: Config | string): Promise<HTMLScriptElement | undefined>;
